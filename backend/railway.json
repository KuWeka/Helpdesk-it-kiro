{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "npx prisma migrate deploy --schema=src/prisma/schema.prisma && npm run start",
    "healthcheckPath": "/api/health"
  }
}
